source "https://rubygems.org"

# Matches what GitHub Pages actually runs, so a local build behaves like
# the live one. The site is normally built by GitHub Pages on push, so
# this file is only needed if you want to preview locally:
#
#   bundle install
#   bundle exec jekyll serve
#
gem "github-pages", group: :jekyll_plugins
